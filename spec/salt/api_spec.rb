require 'spec_helper'

describe Salt::Api do
  it 'has a version number' do
    expect(Salt::Api::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
